<div class='flex justify-between items-center mt-3'>
  <div class="flex items-center space-x-2">
      <%= image_tag 'avatar.svg', class: "rounded-full w-6" %>
      <div>
        <p class='font-semibold mb-0 text-xs'><%= user.username %></p>
        <p class='font-light mb-0 text-xs'><%= user.full_name %></p>
      </div>
    </div>

  <%= link_to 'Follow', posts_path, class: 'text-xs'%>
</div>
